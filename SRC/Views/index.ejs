
<%- include('include/head')%>
<%- include('include/nav')%>
  
<div class="container">
  <div class="row">

    <div class="col-lg-2"></div>

    <div class="col-lg-8 my-3">
      <h1 class=" text-center">Agenda</h1>
      <p class="text-center lead">Contatos</p>

      <%- include('include/message') %>
      <% if(contatos.length > 0) {%>
      <div class="responsive-table">
        <table class="table my-3">
          <% contatos.forEach(contato =>{ %>
          <tr>
            <td><%= contato.nome %></td>
            <td><%= contato.sobrenome %></td>
            <td><%= contato.email %></td>
            <td><%= contato.telefone %></td>
            <td><a href="/contato/index/<%= contato._id %>">Editar</a></td>
            <td><a class="text-danger" href="/contato/delete/<%= contato._id %>">Excluir</a></td>
          </tr>
          <% }) %>
        </table>
        <div class="text-center">
          <a class="btn btn-primary mt-3 " href="/contato/index">Cadastrar contato</a>
        </div>
         
      
      </div>
      <% } else {%>
        <p class="text-center lead">Não existem contatos na sua agenda.</p>
        <div class="text-center">
          <a class="btn btn-primary mt-3 " href="/contato/index">Cadastrar contato</a>
        </div>
        <%}%>
   </div>

    <div class="col-lg-2"></div>

  </div>
</div>


<%- include('include/footer')%>
    <!-- Optional JavaScript; choose one of the two! -->

    <!-- Option 1: Bootstrap Bundle with Popper -->
  